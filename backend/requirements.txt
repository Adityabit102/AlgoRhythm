# ── Web / API (needed to boot, mock mode works with just these) ──
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.0
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
slowapi>=0.1.9
httpx>=0.27

# ── External integrations ──
spotipy>=2.23
boto3>=1.34

# ── ML inference (only imported when USE_MOCK_MODEL=0) ──
# xgboost-cpu: CPU-only build (~5MB) — the default `xgboost` wheel bundles ~300MB
# of CUDA libs that overflow a t3.micro disk and aren't needed for inference.
xgboost-cpu>=2.0
shap>=0.44
scikit-learn>=1.4
joblib>=1.3
pandas>=2.1
numpy>=1.26

# Testing
pytest>=8.0
